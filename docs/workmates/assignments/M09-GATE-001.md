# M09-GATE-001 — 收口 bootstrap 与全量门禁

`worker_backend`，请接手 M09-GATE-001。

工作目录：`/mnt/d/Workstation/Projects/cs-board`

遵照：

1. `docs/workmates/team-contract.md`
2. `docs/workmates/board.md`
3. `docs/Mountain/29-voice-provider-and-infographic-plan.md` §§5–6

回执写入：`docs/workmates/agent-receipts/m09-gate-001-worker.md`

本轮动作：

- 先读取当前 dirty diff；不得重置、删除或覆盖其他未提交变更。
- 只处理当前性能优化导致的两个 bootstrap 测试失败，限定文件为 `webapp/mountain_server.py`、`csboard/adapters/filesystem/service_registry.py`、`csboard/adapters/provider_factory.py` 及其直接测试。
- 保留已修复的 `test_mountain_api.py` 四个 stage endpoint 测试；不得新增 skip 或删除断言。
- 优先跑 affected tests；仅在 affected tests 成功后跑一次 `python -m pytest -q`。若仍超过 180 秒，只报告真实原因，不扩大到无关模块。
- 更新 `docs/workmates/board.md` 为 `verification`，并写明命令、退出码、数量、耗时、变更文件与剩余风险。

完成门槛：

- 两个 bootstrap 测试与四个 stage endpoint 测试均退出 0、无 skip。
- 无新增 M09 以外模块改动；无真实 Remotion 执行。
- worker 回执已落盘；不得提交。

停止条件：

- 15 分钟内不能获得上述最小结果时，停止继续扩大改动，在回执中记录复现、已尝试和精确阻塞，交回 PM。
