---
name: visual-renderer
description: Run or inspect visual clip rendering in a cs-board project. Use after illustrations and timing are ready; do not perform final audio/video composition.
---

Invoke the CLI with the existing project/run. Clip durations come only from the shared Timeline; never rerun alignment or estimate duration from text. For M04/M06 standard work, the selected engine is whiteboard.

Return structured validation failures for dimensions, fps, duration, or reveal checks. Do not implement renderer subprocess control in this Skill.
