# Security

请勿在 Issue、日志、截图或提交记录中公开 API Key、参考音频、生成任务目录及其他私人素材。

项目把本地配置写入 `settings/`，把任务数据和结果写入 `outputs/`；两者默认不会被 Git 跟踪。旧运行目录只可暂存于忽略的 `archive/legacy-runtime/`。如果发现安全问题，请通过仓库维护者提供的私密联系方式报告，不要先创建公开 Issue。
