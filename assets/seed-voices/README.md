# Migrated voice examples

These WAV files are the product-controlled copies of the frozen WebUI baseline's
`SEED_VOICES`. The original examples came from the local IndexTTS examples
registry. `voice_*.wav` are the eleven playable voice assets; `emo_*.wav` are
the two referenced emotion clips. Runtime installation reads only this directory
and copies content into the configured Asset Repository data directory.
